# fail2ban filter for ResumeXray auth failures
# Matches JSON-structured log lines from PM2
# Example: {"level":"WARN","message":"POST /auth/login","status":401,"ip":"1.2.3.4"}

[Definition]
failregex = "ip":"<HOST>".*"status":(?:401|403).*"path":"/auth/
ignoreregex =
